# scripts/
> L2 | Parent: ../AGENTS.md

## Members

- `verify-device.mjs`: the manifest's device verification hook. Exits 0 pass, 1 fail, 2 blocked.
- `smoke.sh`: host smoke run.
- `replay-wav.mjs`: replays a stored WAV through the ASR path.

[PROTOCOL]: 变更时更新此头部，然后检查 CLAUDE.md
